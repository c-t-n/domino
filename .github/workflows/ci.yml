name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}

      - name: Sync (all extras + groups)
        run: uv sync --all-extras --all-groups

      - name: Lint (ruff)
        run: uv run --no-sync ruff check .

      - name: Format (ruff)
        run: uv run --no-sync ruff format --check .

      - name: Type check (ty)
        run: uv run --no-sync ty check

      - name: Tests (pytest)
        run: uv run --no-sync pytest -q

  integration:
    name: integration (live brokers)
    runs-on: ubuntu-latest
    services:
      rabbitmq:
        image: rabbitmq:3-alpine
        ports:
          - 5672:5672
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      # Redpanda speaks the Kafka protocol, boots in seconds and needs no
      # ZooKeeper — a full Kafka image would dominate this job's runtime.
      redpanda:
        image: redpandadata/redpanda:latest
        ports:
          - 9092:9092
        options: >-
          --health-cmd "rpk cluster info"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      RABBITMQ_URL: amqp://guest:guest@localhost:5672/
      KAFKA_BOOTSTRAP_SERVERS: localhost:9092
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: "3.12"

      - name: Sync (all extras + groups)
        run: uv sync --all-extras --all-groups

      # A broker that never came up would leave every test skipped and the job
      # green, which is exactly the failure this job exists to catch.
      - name: Check the brokers are reachable
        run: |
          python - <<'PY'
          import socket
          for name, port in (("RabbitMQ", 5672), ("Redpanda", 9092)):
              with socket.create_connection(("localhost", port), timeout=30):
                  print(f"{name} is accepting connections on {port}")
          PY

      - name: Integration tests
        run: uv run --no-sync pytest -q -m integration -ra

  docs:
    name: docs (strict build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: "3.12"

      - name: Build docs
        run: uv run --group docs mkdocs build --strict
